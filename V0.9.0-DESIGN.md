# V0.9.0 設計文件:持股 Tab(美股配置追蹤+再平衡)

> **🟢 V0.9.0 已實作完成** — 此文件保留給回顧設計決策用。實際差異:
> - 嘉信再平衡簡化成 **7 檔**(VTI 24%/VGK 18%/VPL 12%/VWO 6% + VDE 3%/ICLN 2%/URNM 1% = 66%),原設計 9 檔的 O 5%/TLT 3.24% 拿掉(Sela 動工時決定)
> - 嘉信 LENZ 確認 4000 股(原設計問 2000/4000/6000)
> - 第一證券確認 TLT 600 + LENZ 2000 + 現金 USD 2,319
> - 嘉信沒有 TLT(原設計把第一的 TLT 600 誤放嘉信)
> - 加權年化驗算:嘉信 8.40%(目標 8.42%,差 0.02pp 在精度容差)/ 永豐 4.67%(完全吻合)
> - 加 import_holdings.py(Excel fund_flows 匯入腳本)
>
> 詳細看 CLAUDE.md「⚠️ V0.9.0 升版必讀」。

> **這份文件是 V0.8.2 的核心交付**,給下次接手 V0.9.0 的 Claude 用。
> 設計已跟 Sela 對齊完成,**直接動工**,不要重跑討論。
> Sela 的 5 個設計問題答案見「七、設計決策追溯」。

---

## 一、範圍

新增「持股」Tab 在主導覽列(總覽 / 歷史 / 代管 / **持股** / 設定)。
涵蓋三個美股券商(對應 Ledger 兩個既有帳戶):

| 券商 | Ledger 對映 | 持股檔數 | 再平衡 | 資金流 | 加權報酬 |
|---|---|---|---|---|---|
| 嘉信 Charles Schwab | td_us(本版改名「美國券商」)| ~30 檔 | ✓ 啟用 | ✓ 17 筆 | ✓ |
| 第一證券 Firstrade | td_us(同上)| 2 檔(TLT、LENZ)+ 現金 | ✗ | (無資料) | (無資料) |
| 永豐美股(複委託) | sinopac_us | ~13 檔 | ✗ | ✓ 72 筆 | ✓ |

**✗ 第一證券持股股數 Sela 還沒給,動工前必問**。

---

## 二、資料模型

整段加進 `DATA.holdings`:

```js
holdings: {
  // === 1. 券商定義 ===
  brokers: [
    { id: 'schwab',    name: '嘉信',    ledger_id: 'td_us',      currency: 'USD', rebalance: true  },
    { id: 'firstrade', name: '第一證券', ledger_id: 'td_us',      currency: 'USD', rebalance: false },
    { id: 'sinopac',   name: '永豐美股', ledger_id: 'sinopac_us', currency: 'USD', rebalance: false },
  ],

  // === 2. 持股清單(三家合在一個陣列)===
  // 同一 ticker 在不同 broker 是不同筆(TLT 在三家都有)
  positions: [
    // tier: 'core' / 'satellite' / 'stock' / 'speculative'
    // 'core' = 全球 ETF 主配置(VTI/VGK/VPL/VWO/O)
    // 'satellite' = 衛星配置(TLT/VDE/ICLN/URNM/QYLD)
    // 'stock' = 個股
    // 'speculative' = 槓桿/投機 ETF(BITU/CONY/TQQQ/TSLY/MSTY 等)
    { symbol: 'VTI',   broker: 'schwab',    shares: 411,   tier: 'core' },
    // ... 嘉信 ~30 檔
    { symbol: 'TLT',   broker: 'firstrade', shares: ???,   tier: 'satellite' },  // ✗ 待 Sela 補
    { symbol: 'LENZ',  broker: 'firstrade', shares: ???,   tier: 'stock' },      // ✗ 待 Sela 補
    { symbol: 'BRK.B', broker: 'sinopac',   shares: 74.30, tier: 'stock' },
    // ... 永豐 ~13 檔
  ],

  // === 3. 預定比例(只有嘉信)===
  // 用樹狀分群:每個群組總比例 + 群組內各 ticker 比例
  // 嘉信 Excel R3-R9 + R23-R31 結構推導
  target_allocation_schwab: {
    // 平面儲存(展開到 ticker 層級),好計算
    // 各值 = 占嘉信「股票部位」總額的 %(現金不算)
    VTI:  0.24,    // 全球 60% × 40%
    VGK:  0.18,    // 全球 60% × 30%
    VPL:  0.12,
    VWO:  0.06,
    O:    0.05,    // 房市
    TLT:  0.0324,  // 美債(從 Excel R5 J 欄)
    VDE:  0.03,    // 能源 6% 拆 VDE 50%
    ICLN: 0.02,    // 能源 6% 拆 ICLN 33%
    URNM: 0.01,    // 能源 6% 拆 URNM 17%
    // 個股 / 投機 不設預定%(自由部位)
    // Sela 進設定頁微調
  },

  // === 4. 現金 ===
  cash_by_broker: {
    schwab:    32663,  // USD
    firstrade:  2319,
    sinopac:    2948,
  },

  // === 5. 匯率 ===
  fxrate: 31.316,
  fxrate_updated_at: '2026-05-08',  // 手動,不抓 API

  // === 6. 資金流歷史 ===
  // 給長期報酬率分析用(加權平均匯率、加權年化報酬)
  fund_flows: {
    schwab: [
      { date: '2014-04-29', amount_usd: 99000,  fxrate: 30.25  },
      { date: '2014-04-30', amount_usd:  3062,  fxrate: 30.25  },
      // ... 17 筆從嘉信 Excel「資金匯入運算」分頁匯入
    ],
    sinopac: [
      { date: '2018-08-08', amount_usd: 130000, fxrate: 30.622 },
      { date: '2022-09-20', amount_usd:  -2921, fxrate: 31.354 },  // 負數 = 匯回台灣
      // ... 72 筆從永豐 Excel「美元匯率」分頁匯入
    ],
    firstrade: [],  // 沒資料(可能只有從 schwab 轉過去)
  },

  // === 7. 股價快取 ===
  // API 抓回來存這裡,UI 顯示時優先用 quotes,API 失敗時 fallback 用快取
  quotes: {
    'VTI':  { price: 362.87, at: '2026-05-08T10:30Z' },
    'VGK':  { price:  88.12, at: '2026-05-08T10:30Z' },
    // ...
  },
}
```

### 資料模型上的對齊檢查

跟 Ledger 既有帳戶對齊:
- 嘉信 + 第一 持股市值總和 + 兩家現金 → 對齊 `td_us`(萬 TWD = USD × fxrate ÷ 10000)
- 永豐 持股市值 + 永豐現金 → 對齊 `sinopac_us`
- 差 > 1% 顯示警告,**不主動覆寫 Ledger**(凍結機制精神:既定事實優先)

### 資料模型上的注意事項

1. **`tier` 欄位是嘉信獨有**:第一/永豐的 positions 也有 tier 欄位但 UI 不照 tier 分群顯示(因為 tier 只對「再平衡建議」有意義),這兩家就用「ETF / 個股」單純兩區
2. **同 ticker 不同 broker 是不同筆**:TLT 在三家都有,就是 3 筆 positions
3. **股數可以是小數**:永豐有 BRK.B 74.3 股、QQQ 102.5 股(主追蹤 72.5 + 定期定額 30)等碎股

---

## 三、UI 結構(對齊 Ledger 黑底金字)

```
┌──────────────────────────────────────┐
│ 資產札記 LEDGER  v0.9.0              │
│ 總覽 · 歷史 · 代管 · 持股 · 設定      │  ← 加新 tab
├──────────────────────────────────────┤
│                                      │
│ 持股 Holdings                         │
│ [全部] [嘉信] [第一] [永豐]   ← 切換  │
│                                      │
└──────────────────────────────────────┘
```

### 子 tab:全部

```
總美元資產:USD 2,247,000
總台幣資產:TWD 7,036 萬(× 31.316)
匯率:31.316  最新更新:2026-05-08
[編輯匯率] [刷新所有股價]

三家分布:
┌─ 嘉信 ────┬─ 第一 ────┬─ 永豐 ────┐
│ 5,000 萬   │ 7 萬       │ 2,016 萬   │
│ ✓ 對齊     │ —          │ ✓ 對齊     │
└────────────┴────────────┴────────────┘
```

### 子 tab:嘉信

```
┌── 對齊 Ledger ──┐
│ 程式算 5,000 萬 │
│ Ledger 4,996 萬 │
│ ✓ 對齊(差 4 萬,0.08%)│
└─────────────────┘

現金:USD 32,663

▾ 核心配置 60% (摺疊)
   VTI   411   $362.87  USD 14.94 萬
     目前 24.1% / 預定 24% ✓
   VGK   1271  $88.12   USD 11.21 萬
     目前 18.1% / 預定 18% ✓
   ... (5 檔)

▾ 衛星配置 (摺疊)
   TLT   600   $86.08   USD 5.16 萬
     目前 3.4% / 預定 3.24% ✓
   VDE   340   $158.38  USD 5.39 萬
   ICLN  1699  $21.16   USD 3.59 萬
   URNM  204   $64.99   USD 1.33 萬
   QYLD  0     $18.13   USD 0
   ... 

▾ 個股 (摺疊,~10 檔)
   GOOG  50    $397.05  USD 1.99 萬
   MSFT  325   $415.06  USD 13.49 萬
   NVDA  100   $215.22  USD 2.15 萬
   ...

▾ 投機/槓桿 (摺疊,有警告字樣,~7 檔)
   BITU  4050  ...
   CONY  972   $27.19   USD 2.64 萬
   ...

▼ 再平衡建議 (展開)
   🟢 ICLN 多 0.27%(賣 191 股 ≈ USD 4,041)
   🟡 URNM 少 0.17%(買 41 股 ≈ USD 2,665)
   🔴 TLT 缺 0(0% vs 預定 3.24%)? 看實際數字...
   
▾ 資金流(摺疊)
   累計匯入 USD 623,792
   累計匯回 USD 0
   目前市值 USD 1,595,587
   總獲利   USD 974,798(報酬 156.3%)
   加權投資天數 4248.7
   加權年化報酬 8.42%
   加權平均匯率 31.44

[新增持股] [編輯預定%] [新增資金流]
```

### 子 tab:第一

```
現金:USD 2,319

持股:
   TLT   ???  $86.08   USD ???      ← 待 Sela 補股數
   LENZ  ???  $10.06   USD ???      ← 待 Sela 補股數

(無再平衡、無資金流分析)
```

### 子 tab:永豐

```
┌── 對齊 Ledger ──┐
│ 程式算 2,010 萬 │
│ Ledger 2,016 萬 │
│ ⚠ 差 6 萬(0.3%)│
└─────────────────┘

現金:USD 2,948

持股(13 檔,不分 tier):
   BRK.B  74.30  $475.94  USD 3.54 萬
   GOOG   30.15  $397.05  USD 1.20 萬
   ISRG   27.62  $450.06  USD 1.24 萬
   NVDA   26.79  $215.22  USD 0.58 萬
   O      200.58 $61.92   USD 1.24 萬
   QQQ    102.5  $711.06  USD 7.29 萬   ← 主追 72.5 + 定期 30 合併
   TLT    360.66 $86.08   USD 3.10 萬
   AMZN   25     $272.68  USD 0.68 萬
   MSFT   20     $415.06  USD 0.83 萬
   UBER   22.39  $75.45   USD 0.17 萬
   CONY   80     $27.19   USD 0.22 萬
   LENZ   2200   $10.06   USD 2.21 萬
   QYLD   848    $18.13   USD 1.54 萬   ← 預賣 350 股 → TWD 100 萬
   BIL    0      $91.46   USD 0(只追蹤,沒持股)

▾ 資金流(摺疊)
   累計匯入 USD 501,008(72 筆 2018-2026)
   累計匯回 ???(從 Excel 算負數加總)
   目前市值 USD 643,848
   總獲利   USD 142,840(報酬 28.5%)
   加權投資天數 2005
   加權年化報酬 4.67%

(無再平衡)
```

### 設定 Tab 新增區塊

設定頁加「持股設定」段落:
- 增刪持股(symbol、broker、shares、tier)
- 修改預定 %(只嘉信)
- 編輯資金流(每月匯入/匯出時用)
- 切換股價 API 來源(預設 Yahoo,可切 Stooq 之類)
- 「強制重新抓股價」按鈕(類似 V0.7「強制重新凍結」逃生口)

---

## 四、加權報酬演算法

從兩份 Excel 推導出來的公式(Excel 原本的算法):

```javascript
function computeBrokerStats(broker_id, fund_flows, today_market_value_usd, today_fxrate) {
  const flows = fund_flows[broker_id] || [];
  const today = new Date();
  
  // 對每筆資金流
  let total_amount_usd = 0;        // Σ amount_usd(含負數)
  let total_amount_twd = 0;        // Σ amount_usd × fxrate(含負數)
  let weighted_days_sum = 0;       // Σ amount × days
  let weighted_fxrate_days_sum = 0;// Σ amount × fxrate × days
  
  for (const f of flows) {
    const flow_date = new Date(f.date);
    const days = (today - flow_date) / (1000 * 60 * 60 * 24);
    
    total_amount_usd      += f.amount_usd;
    total_amount_twd      += f.amount_usd * f.fxrate;
    weighted_days_sum     += f.amount_usd * days;
    weighted_fxrate_days_sum += f.amount_usd * f.fxrate * days;
  }
  
  // 加權投資天數(USD 平均持有天數)
  const weighted_days = weighted_days_sum / total_amount_usd;
  
  // 加權平均匯率(持有期間)
  const weighted_fxrate = weighted_fxrate_days_sum / weighted_days_sum;
  
  // 損益
  const total_market_twd = today_market_value_usd * today_fxrate;
  const profit_usd = today_market_value_usd - total_amount_usd;
  const profit_twd = total_market_twd - total_amount_twd;
  const profit_ratio = profit_usd / total_amount_usd;
  
  // 加權年化報酬
  const annualized = Math.pow(1 + profit_ratio, 365 / weighted_days) - 1;
  
  return {
    total_amount_usd,         // 總匯出 USD
    total_amount_twd,         // 總匯出 TWD
    today_market_usd: today_market_value_usd,
    today_market_twd: total_market_twd,
    profit_usd,               // 總獲利 USD
    profit_twd,               // 總獲利 TWD
    profit_ratio,             // 獲利比率
    weighted_days,            // 加權投資天數
    weighted_fxrate,          // 加權平均匯率
    annualized_return,        // 加權年化報酬
  };
}
```

**驗算**(動工時必跑這個確保算法對):
- 嘉信 Excel R3-R9 + R10-R14:
  - 總匯出 623,792 USD,總美金 1,595,587 USD
  - 獲利 USD 974,798,比率 156.3%
  - 加權天數 4248.7,加權年化 8.42%
- 永豐 Excel R3-R8 + R10-R12:
  - 總匯出 501,008 USD,總美金 643,848 USD
  - 獲利 USD 142,840,比率 28.5%
  - 加權天數 2005,加權年化 4.67%

寫完算法後,先用上面數字對 Excel 驗算對齊,**0 誤差**才繼續寫 UI。

---

## 五、股價 API 設計

### 三層 fallback

```javascript
async function getQuote(symbol) {
  // Layer 1: 直連 Yahoo Finance(免 key,可能 CORS 擋)
  try {
    const r = await fetch(`https://query1.finance.yahoo.com/v8/finance/chart/${symbol}?range=1d`);
    if (r.ok) {
      const j = await r.json();
      return j.chart.result[0].meta.regularMarketPrice;
    }
  } catch (e) { /* fall through */ }
  
  // Layer 2: 反向代理(jina + corsproxy 雙備援)
  const proxies = [
    `https://r.jina.ai/https://query1.finance.yahoo.com/v8/finance/chart/${symbol}`,
    `https://corsproxy.io/?https://query1.finance.yahoo.com/v8/finance/chart/${symbol}`,
  ];
  for (const url of proxies) {
    try {
      const r = await fetch(url);
      if (r.ok) {
        const j = await r.json();
        return j.chart.result[0].meta.regularMarketPrice;
      }
    } catch (e) { /* try next */ }
  }
  
  // Layer 3: 失敗,fallback 用快取
  return null;  // caller 用 DATA.holdings.quotes[symbol] fallback
}
```

### 抓股價的時機

- 進「持股」tab 時,自動刷新一次(背景跑,不擋 UI)
- 「刷新所有股價」按鈕,手動觸發
- 各 ticker 並行抓(`Promise.allSettled`)
- 失敗的 ticker 在 UI 標 ⚠ 「用快取價(時間戳)」

### 抓股價數量考量

三家合計約 35-40 檔 ticker(嘉信 30 + 永豐 13,扣掉重複 TLT/LENZ 等),一次刷新 30+ 個 HTTP 請求。Yahoo 對單 IP 沒 rate limit(只是偶爾擋),但代理服務 jina/corsproxy 可能有(每分鐘幾十次)。

**設計上**:
- 重複 ticker 只抓一次(`new Set(positions.map(p => p.symbol))`)
- 抓回來存 `DATA.holdings.quotes`,所有 broker 共用
- 「自動刷新」不擋 UI,失敗 silent(只在 quotes 標時間戳老舊)

---

## 六、Excel 匯入腳本設計(import_excel.py 升級)

`import_excel.py` 既有功能(Sela 月度資產 Excel)保留不動,**新增兩個獨立腳本**或在原檔加分支:

### 嘉信版(import_us_brokers.py 或 import_excel.py 加旗標)

來源:`前進美國券商_*.xlsx`

讀分頁:
- `配置`:R3-R9 = 核心配置 5 檔、R10-R14 = 衛星配置 5 檔(TLT/VDE/ICLN/URNM/QYLD)、R12-R16 = 「目前 ETF」(其實是當前持股)、R18-R45(I 欄)= 個股 + 投機(~25 檔)
- `配置`:R23-R24 = 嘉信現金 / 第一現金
- `資金匯入運算`:R2 起 = 17 筆資金流(date / amount / fxrate)

排除:
- ✗ `成本記錄`(Sela 不要記成本)
- ✗ `欲購回`(篩選工具,不入)
- ✗ `2024 卷股賭賽` / `2022 小賭局 1`(賭局,不入)
- ✗ `美股退稅`(邊緣)
- ✗ `獲利率`(歷史對比 S&P500,進階分析)

輸出:加進 `DATA.holdings.positions[broker='schwab']` + `cash_by_broker.schwab` + `fund_flows.schwab`。

第一證券持股(TLT、LENZ 兩檔)Excel 沒明確分,**手動補入** `DATA.holdings.positions[broker='firstrade']`。

### 永豐版(import_sinopac.py)

來源:`永豐複委託_*.xlsx`

讀分頁:
- `複委託配置`:R3-R12 = 主追蹤 8 檔、R18-R21 = 投資對照 4 檔(AMZN/QQQ/MSFT/UBER,**已持有要合併**)、R20 = QYLD 848 股 / 預賣計畫
  - **特例:QQQ 主追蹤 72.5 股 + 投資對照 30 股 → 合併 102.5 股**
- `複委託配置`:R4 = 現金 USD 2,948
- `美元匯率`:R2 起 = 72 筆資金流

注意:Excel R1 寫「TD Ameritrade 867238065」是抄錯,實為永豐複委託。

輸出:加進 `DATA.holdings.positions[broker='sinopac']` + `cash_by_broker.sinopac` + `fund_flows.sinopac`。

---

## 七、設計決策追溯(Sela 5 個問題答案)

| 問題 | 答案 | 影響設計 |
|---|---|---|
| 1. 永豐 Excel TD 帳號 867238065 | **抄錯**(複製貼上沒清) | CLAUDE.md 業務事實段加一行 |
| 2. 永豐主追蹤 + 投資對照怎麼處理 | **合併當一個清單**(Excel 結構亂,實際是一個合計) | 永豐持股 = 主 8 + 投資對照 4 + QYLD 1 = 13 檔 |
| 3. AMZN/QQQ/MSFT/UBER | **現在持有**(Excel 主清單漏了) | 加進永豐持股 |
| 4. 永豐匯率歷史 72 筆 | **要保留**(Sela 有時把錢領出來用,要算實際賺賠) | fund_flows 完整保留 + 加權報酬演算法 |
| 5. 預定比例兩家分開設? | **只嘉信需要再平衡**,其他兩家 Sela 有自己的作法 | UI 上 schwab tab 才有「再平衡建議」+「預定 %」設定;firstrade/sinopac 不顯示 |

額外決策:
- **td_us 帳戶名稱改「美國券商」**(因為實際是嘉信+第一,「TD」是歷史包袱;TD 已併入嘉信),id 維持 `td_us` 不動
- **第一證券有 2 檔持股**(TLT、LENZ)— Sela 後補;Excel 沒分配
- **不記成本** — 只看現況(股數 × 即時股價)
- **股價走 API**(Yahoo + jina/corsproxy fallback)
- **嘉信 LENZ 股數**:Excel 寫 2000 / 4000 兩處,動工前要 Sela 確認

---

## 八、CLAUDE.md 該動的地方(V0.9.0 動工時)

### 業務事實段加:

```
- td_us 顯示名稱「美國券商」,實際涵蓋嘉信(主) + 第一證券(副),id 維持 td_us 不動(改 id 會破壞 6 年快照)
- 永豐 Excel R1 標題「TD Ameritrade 867238065」是抄錯,實為永豐複委託
- 永豐 QQQ 102.5 股 = 主追蹤 72.5 + 定期定額 30(碎股因兩種買法)
- 第一證券持股:TLT、LENZ 兩檔(股數 Sela 提供)+ 現金 USD 2,319
- 加權年化報酬演算法見 V0.9.0-DESIGN.md 第四章(嘉信 8.42% / 永豐 4.67%)
```

### 升版必讀(這次 schema 加大欄位 + 新表,要寫):

```
## ⚠️ V0.9.0 升版必讀

V0.9.0 加 holdings 整個欄位 + 新增「持股」tab。
- DATA 加 holdings 欄位(brokers / positions / fund_flows / target_allocation_schwab / quotes 等)
- mergeSettings 要補 holdings 欄位的補齊邏輯(類似 V0.7.1 坑 #27)
- td_us 的 name 改為「美國券商」(id 不動)

部署選項:
- 選項 A:推 V0.9.0,localStorage 自動 mergeSettings 補齊
- 選項 B:升前先匯出 JSON 備份(防萬一)
```

### 加坑 #31(預期會踩):

```
31. holdings 升級時 mergeSettings 沒補(預期會踩,V0.9.0 預防)
    - 症狀:升 V0.9.0 後 holdings 是 undefined,持股 tab 渲染 fail
    - 原因:V0.7.1 已踩過 #27 mergeSettings 教訓
    - 做法:loadData() 在 mergeSettings 後加 `if (!stored.holdings) stored.holdings = DEFAULT_DATA.holdings`
```

### 一句話總結重寫(範例):

```
V0.9.0 加「持股 Tab」:三家美股券商(嘉信+第一+永豐)個別追蹤 ETF/個股、嘉信再平衡建議、加權年化報酬計算、Yahoo Finance API 抓股價。td_us 改名「美國券商」(id 不動)。下版第一優先看實際使用反饋微調。
```

---

## 九、新對話開始時 Claude 應該做的事

1. **讀** CLAUDE.md(整篇)+ V0.9.0-DESIGN.md(這份)
2. **問 Sela**:
   - 第一證券 TLT 股數?LENZ 股數?
   - 嘉信 LENZ 是 2000 還是 6000 還是 8000(2000+6000)?
3. **動工順序**(避免 context 爆掉):
   - Step 1:資料模型 + DEFAULT_DATA 加 holdings + mergeSettings 補齊邏輯
   - Step 2:加權報酬演算法 + 用 Excel 數字驗算對齊(不對就停下來查)
   - Step 3:嘉信 + 永豐 Excel 匯入腳本
   - Step 4:股價 API 抓 + 三層 fallback
   - Step 5:持股 tab UI 結構 + 樣式(對齊 Ledger 黑底金字 / JetBrains Mono)
   - Step 6:渲染邏輯(三家分開 tab + 摺疊群組 + 再平衡建議)
   - Step 7:設定 tab 加「持股設定」區塊
   - Step 8:td_us 改名「美國券商」
   - Step 9:文件更新(CLAUDE.md 加坑 / 業務事實 / 升版必讀 / 一句話總結;README 加 V0.9.0;SELA-handoff 看是否要補)
   - Step 10:整合測試 + 打包

4. **預估規模**:+1200 行(目前 ~3155 → V0.9.0 約 ~4400 行)。看起來不少,但設計已定稿,寫起來相對線性。

5. **打包前檢查**:Excel 12 條公式對齊不變(這版不應該動到月度試算)、加權報酬演算法對齊 Excel 數字、持股對齊 Ledger 帳戶。

---

## 十、不做的清單(明確不要碰)

- ✗ 嘉信 Excel 的「成本記錄」18 檔(Sela 不記成本)
- ✗ 嘉信 Excel 的「欲購回」57 檔(選股工具)
- ✗ 嘉信 Excel 的「2024 卷股賭賽」「2022 小賭局 1」(跟資產無關)
- ✗ 嘉信 Excel 的「美股退稅」(太邊緣)
- ✗ 嘉信 Excel 的「獲利率」(28 個月對比 S&P500,進階分析)
- ✗ 永豐 Excel 拆「主追蹤 + 雜部位」兩層(合併一個清單)
- ✗ 第一證券再平衡(只有 2 檔,Sela 不需要)
- ✗ 永豐再平衡(Sela 有自己的作法)
- ✗ 月度持股快照(holdings 是即時的,不像 Ledger 月度快照,**不存進 snapshot**)
- ✗ 自動覆寫 Ledger td_us / sinopac_us 帳戶(凍結機制精神)

如果 Sela 哪天又想要其中一條,再開 V0.9.x 加。

---

> 設計於 2026-05-08,Sela + Claude 對齊 5 輪討論後定稿。
> 動工時遇到設計問題,先回頭讀這份,再問 Sela,不要自己擅自決定。
